name: Build APK
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.5'
      - run: |
          echo "Saving your ZFANZ code..."
          cp -r lib /tmp/lib_real
          cp pubspec.yaml /tmp/pubspec_real.yaml
          echo "Deleting broken android..."
          rm -rf android
          echo "Creating new android with new Gradle..."
          flutter create --platforms=android --project-name zfanz_electronics --org com.zfanz.
          echo "Restoring your ZFANZ code..."
          rm -rf lib
          cp -r /tmp/lib_real lib
          cp /tmp/pubspec_real.yaml pubspec.yaml
          flutter pub get
          flutter build apk --release
      - uses: actions/upload-artifact@v4
        with:
          name: ZFANZ-APK
          path: build/app/outputs/flutter-apk/app-release.apk
